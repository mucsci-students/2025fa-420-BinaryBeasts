"""
This file replaced the previous `test_timeslot_gui.py` during a merge.

The canonical tests were merged into `tests/test_timeslots.py`. This file
is kept as a note and is not collected by pytest because it doesn't start
with the `test_` prefix.
"""
