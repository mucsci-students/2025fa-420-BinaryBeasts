[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scheduler"
version = "0.1.0"
description = "Course Scheduler Project - A constraint-based academic scheduling system with CLI and GUI interfaces"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
keywords = ["scheduler", "course-scheduler", "constraint-solver", "academic", "timetable"]
authors = [
  {name = "Andrew Delich"},
  {name = "Collin Donnan"},
  {name = "Kenner Jimenez"},
  {name = "Naomi Ermold"},
  {name = "Patrick Kreibick"},
  {name = "Sophia Koziar"},
  {name = "Tyler Brown"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Education",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "course-constraint-scheduler",
    "pyqt5>=5.15.11",
    "pytest>=8.4.2",
    "pytest-codecov>=0.7.0",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.1",
    "ty>=0.0.1a23",
    "langchain>=0.3.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "langgraph>=0.2.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
gui = ["pyqt5>=5.15"]
dev = ["pytest>=8.0"]
all = ["pyqt5>=5.15", "pytest>=8.0"]

[project.scripts]
scheduler = "app:main"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src"]


[tool.uv]
# Help uv resolve platform-specific wheels correctly on Windows.
# This prevents uv from choosing linux/mac-only Qt subpackages like `pyqt5-qt5`
# when running on win_amd64.
required-environments = [
  "sys_platform == 'win32' and platform_machine == 'AMD64'",
]


